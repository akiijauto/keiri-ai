# 振り返り

開発中に起きた課題と、その解決策を記録します。
このファイルは**そのままnote/Zenn記事の材料**になります。困ったことは小さくても書き残してください。

## 書き方のテンプレート

```
### YYYY-MM-DD ｜ 課題のタイトル

**何が起きたか**

**なぜ起きたか（原因）**

**どう解決したか**

**次に活かせること**
```

---

## 2026-08-09 ｜ プロジェクト開始

**何が起きたか**
「経理×AI」を強みドメインと定め、その実力証明としてポートフォリオ開発を開始。

**判断したこと**
- 会計ソフトはfreeeを想定（AI連携がしやすいため）。勘定科目マスタ・CSV形式もfreee準拠
- APIはGeminiを指定（ポートフォリオ用途のためコスト抑制を優先）
- ホスティングはVercelではなく自前契約のXserver VPSに一本化
- 公開範囲はBasic認証で制限（ID: eigyo）
- 正式名称は中身が固まってから決める

**次に活かせること**
技術選定の理由を最初に言語化しておくと、面接や商談でそのまま話せる。

---

## 記録予定の観点（開発中に埋めていく）

- [ ] 画像PDFの読取精度はどうだったか。前処理（解像度・二値化）で改善したか
- [ ] 仕訳推定のプロンプトをどう改善したか。何回目で実用レベルになったか
- [ ] 勘定科目の推定を誤りやすいパターンは何か
- [ ] 税区分（課税／非課税／不課税／軽減税率）の判定でつまずいた点
- [ ] freeeのCSV仕様で想定と違った点
- [ ] Xserver VPS上での構築でつまずいた点（既存サービスとの共存、メモリ、ポート）
- [ ] Gemini APIのコストは実際いくらだったか
- [ ] LLM抽象化レイヤーは実際に役立ったか

---

## 2026-08-09 ｜ 日本語PDFのフォント埋め込みでラスタライズに失敗

**何が起きたか**
サンプル請求書PDFをreportlabで生成し、pdftoppmで画像化しようとしたところ
`Unknown font tag 'F2'` `No font in show` エラーが多発し、文字が消えた。

**なぜ起きたか（原因）**
日本語表示に `UnicodeCIDFont("HeiseiKakuGo-W5")` を使っていた。
CIDフォントはPDFに埋め込まれず、閲覧側の環境にフォントがある前提になる。
サーバー上にそのフォントが無いため、画像化時にフォント解決に失敗した。

**どう解決したか**
埋め込み可能なTTFに変更した。

```python
from reportlab.pdfbase.ttfonts import TTFont
pdfmetrics.registerFont(TTFont("JPGothic", "/usr/share/fonts/truetype/fonts-japanese-gothic.ttf"))
```

**次に活かせること**
- 日本語PDFを生成して**さらに画像化・OCRする**用途では、CIDフォントではなくTTF埋め込みを使う
- 「画面で見えている＝正しく生成できている」ではない。後工程（画像化）まで通して確認する
- これは実務でも同じ。帳票PDFを作って外部に渡す場合、相手の環境にフォントが無い前提で設計する

---

## 2026-08-09 ｜ 実装1日目に完成したもの

**作ったもの**
- サンプル請求書5パターン（通常課税/軽減税率混在/非課税/インボイス番号なし/スキャン画像）
- PDF読取モジュール（テキスト量120文字未満なら画像PDFと判定して画像化に切替）
- 勘定科目マスタ18科目・税区分5種（freee準拠）
- 仕訳推定プロンプト（モデル非依存）
- freee形式CSV出力（貸借一致の検証つき）
- FastAPI 4エンドポイント
- Next.js UI（読取結果表示→修正→承認→CSV出力）
- Nginx設定・systemdユニット・VPSセットアップ手順
- 機能テスト24件（全PASS）

**設計上こだわった点**
- AIの推定根拠を必ず表示（監査証跡の発想）
- 確信度70%未満は「要確認」バッジを出す
- 貸借不一致ならCSV出力ボタンを無効化（誤った仕訳を出させない）
- 電帳法の検索要件を満たすファイル名案を自動生成

**次に活かせること**
「経理が分かっているから入れられた仕様」がどれか、記事にするときの主軸になる。

---

## 2026-08-09 ｜ PORT環境変数が効かず、意図しない全インターフェースで公開されかけた

**何が起きたか**
本番VPSでフロントエンドを systemd で起動したところ、
`Environment=PORT=3100` を指定しているのに **3100 では待ち受けていなかった**。
`curl http://127.0.0.1:3100` は `HTTP=000`（接続できず）。

実際には **3000番ポート**、しかも `127.0.0.1` ではなく
**`*:3000`（全インターフェース）** で待ち受けていた。
これは nginx のBasic認証を経由せず、外部から直接アクセスされうる状態を意味する。

`systemctl status` は `active (running)` と表示されており、
**状態表示だけを見ていたら異常に気づけなかった**。

**なぜ起きたか（原因）**

原因は2つ重なっていた。

1. **package.json のハードコードが環境変数を上書きしていた**

   ```json
   "start": "next start -p 3000"
   ```

   systemd から `PORT=3100` を渡しても、`npm run start` 経由で実行される
   コマンドライン引数 `-p 3000` が優先される。
   環境変数は「渡した」が「効いていなかった」。

2. **`output: "standalone"` と `next start` が非互換**

   `next.config.mjs` に `output: "standalone"` を指定していたため、
   Next.js 自身が警告を出していた。

   ```
   ⚠ "next start" does not work with "output: standalone" configuration.
     Use "node .next/standalone/server.js" instead.
   ```

   さらに standalone の `server.js` は `HOSTNAME` 未指定だと
   `0.0.0.0` で待ち受ける仕様。ここが全インターフェース公開の直接原因。

**どう解決したか**

- systemdユニットの `ExecStart` を `node /opt/keiri-ai/app/.next/standalone/server.js` に変更
- `Environment=HOSTNAME=127.0.0.1` を追加してループバック限定にした
- `package.json` の `start` から `-p 3000` を削除
- standalone には静的ファイルが含まれないため、
  `.next/static` と `public` を standalone 配下へコピーする手順をREADME・setup_vps.shに追加
- 起動後に `ss -tlnp | grep -E '3100|8101'` で待ち受けアドレスを検証する手順を各ドキュメントに明記

なお、問題の時間帯に外部から `http://<IP>:3000` への到達を試したところタイムアウトし、
上流（Xserver側）のパケットフィルタで遮断されていたと考えられる。
ただし **VPS上の ufw は非アクティブ** で、OS側の防御は無く上流頼みの状態だった。

**次に活かせること**

- **設定は「渡した」ではなく「効いた」を確認する。**
  環境変数を書いた＝反映された、ではない。
  `ss -tlnp` で実際の待ち受けアドレスとポートを見るまで信用しない。
- `systemctl status` の `active (running)` は「プロセスが生きている」だけ。
  ラッパー（npm）が生きていても中身が期待どおりとは限らない。**外形確認まで行う。**
- ポート番号だけでなく **バインドアドレス（127.0.0.1 か 0.0.0.0 か）を必ず見る。**
  リバースプロキシで認証をかける構成では、バックエンドが全インターフェースに
  出ていると認証が無意味になる。
- ビルドツールの警告は読む。今回は Next.js が起動ログで正確に警告していたが、
  `active (running)` を見て安心して読み飛ばしかけた。
- 実務でも同じ。「設定したつもり」が最も危ない。設定変更後は必ず観測して確かめる。

---

## 2026-08-09 ｜ サンプル5パターンで実地検証、全件成功

**何が起きたか（検証結果）**

本番VPS上で、サンプル請求書5パターンすべてを `/analyze` に通した。

| パターン | 結果 |
|---|---|
| 01 通常課税（インボイスあり） | 成功 |
| 02 軽減税率8%混在 | 成功 |
| 03 非課税（賃料・保険料） | 成功 |
| 04 インボイス登録番号なし | 成功 |
| 05 画像PDF（スキャン想定） | 成功 |

**エラーなく全件が仕訳案の生成まで到達した。**
画像PDF（スキャン想定）も、画像化してAIに渡す経路で読取に成功している。

**抽出精度**
発行元・インボイス登録番号・請求番号・金額・明細のすべてが正確に抽出できた。
例（01）: 発行元「オフィスサプライ東西株式会社」／登録番号 `T1234567890123`／
請求番号 `INV-2026-0451`／税込 59,400円／明細3行、いずれも原本と一致。

**仕訳の質 — 確信度の付け方が実用的だった**

判断が割れる項目で、確信度を適切に下げていた点が良かった。
「時間指定手数料」では確信度を **90%** に下げ、**代替科目を根拠つきで併記**していた。

100%か0%かではなく「これは人間が見た方がいい」と示せることが、
経理業務では実用上いちばん効く。**AIは案を出すだけ、確定は人間**という
設計方針（D-4）が、確信度の表現と噛み合っている。

**CSV出力**
出力したCSVをExcelで開き、**文字化けなく表示されることを確認**（cp932）。
自動テストでもエンコーディング・CRLF改行・ヘッダー行を検証している（24件全PASS）。

**未検証・今後の課題**
- 誤りやすいパターンの**体系的な洗い出し**は未着手。
  5件はいずれも自作サンプルであり、想定内の請求書しか通していない。
- 実務の請求書は書式のばらつきが大きい。手書き・複数ページ・
  明細が画像化されているものなど、崩れやすい入力での検証が必要。

**次に活かせること**
サンプルは「作った人が想定した範囲」しか含まない。
全件成功は出発点であって、精度の証明にはならない。

---

## 2026-08-09 ｜ モデルは水物である — ハードコードしてはいけない

**何が起きたか**
本番稼働直後、`/analyze` が 500 エラーを返した。
原因はGemini APIの **429 `ResourceExhausted`** が捕捉されずに素通りしていたこと。

調査でモデルごとに状況がまったく違うことが分かった。

| モデル | 結果 |
|---|---|
| `gemini-2.0-flash`（当初の設定） | ❌ 429。**無料枠の割当が `limit: 0`** |
| `gemini-2.0-flash-lite` | ❌ 429 同上 |
| `gemini-2.5-flash` | ❌ 404「no longer available to new users」 |
| `gemini-2.5-flash-lite` | ❌ 404 同上 |
| **`gemini-flash-latest`** | ✅ **成功** |

**なぜ起きたか（原因）**

- `limit: 0` は「使い切った」ではなく **「最初から割当がない」**。
  待っても回復しない。リトライで解決する類の429ではなかった。
- 2.5系の404は「新規ユーザーには提供終了」。
  **昨日まで使えたモデルが、今日から新規には使えない**ということが起きる。
- APIキーや認証には一切問題がなかった（`list_models` は正常に20件返した）。
  「認証エラーではない」と早期に切り分けられたことで、原因究明が早まった。

**どう解決したか**

- モデル名を `GEMINI_MODEL` 環境変数で差し替え可能にし、既定を `gemini-flash-latest` に変更
- プロバイダ固有の `ResourceExhausted` を、共通例外 `LLMRateLimitError` に変換して送出
- APIは 500 ではなく **503** と意味のあるメッセージを返すようにした

**次に活かせること**

- **モデル名をコードにハードコードしない。環境変数で差し替え可能にする。**
  今回の修正で、次に同じことが起きても `.env` の1行で復旧できる。
  コード修正・再デプロイが不要になる差は大きい。
- **モデルは水物**。無料枠の有無・提供終了は予告なく変わる。
  「動いていたモデルが動き続ける」という前提を置かない。
- 429を見たら反射的にリトライを考えがちだが、**`limit: 0` は待っても直らない**。
  エラーの詳細（quota_metric / limit値）まで読むこと。
- LLM抽象化レイヤー（`llm_client.py`）が設計どおり効いた。
  モデル変更もエラー変換も**このファイル1つの修正で完結**し、
  `main.py` にGeminiのSDK固有例外を持ち込まずに済んだ。
  「将来モデルが変わる」は起きるかもしれない話ではなく、**1日で起きた**。
