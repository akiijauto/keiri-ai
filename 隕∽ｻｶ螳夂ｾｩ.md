# 要件定義書

**プロジェクト名（仮）**: 請求書仕訳AI
**作成日**: 2026-08-09
**バージョン**: v1.1
**Notion**: 2026-08-09-経理AIポートフォリオ（請求書仕訳エージェント）要件定義

---

## 1. 背景と目的

「経理×AI」を強みドメインとしたキャリア戦略に基づく、実力証明用ポートフォリオ。
採用担当者・発注者に対し「経理実務を理解したうえでAIシステムを設計・実装できる」ことを示すことを目的とする。

商用化・課金は本フェーズのスコープ外。

## 2. スコープ

### 2.1 MVPで実装する機能

| No | 機能 | 概要 |
|----|------|------|
| F-1 | 請求書アップロード | PDF（テキスト/画像どちらも）を1件アップロード |
| F-2 | AI読取 | 取引先名・請求日・支払期日・金額（税抜/税込/消費税額）・明細摘要・インボイス登録番号を抽出 |
| F-3 | 仕訳案生成 | 借方/貸方の勘定科目推定＋税区分推定＋推定根拠の説明文を生成 |
| F-4 | 承認フロー | 画面上で人間が修正・承認して確定。修正内容は改善データとして保存 |
| F-5 | CSV出力 | freee準拠形式で仕訳CSVをダウンロード |

### 2.2 非スコープ（MVPでやらない）

- 課金機能・マルチテナント化
- 会計ソフトAPIとの直接連携（CSV出力で代替）
- 実在企業の実データ利用（サンプル請求書を自作して使用）
- 複数ファイルの一括処理（1件ずつ）

## 3. 差別化要件（経理実務者ならではの設計）

| No | 要件 | 意図 |
|----|------|------|
| D-1 | 推定根拠の説明を必ず出力 | 監査証跡の発想。「なぜこの科目・この税区分か」を人間が検証できる |
| D-2 | インボイス登録番号の有無チェック | 適格請求書か否かで仕入税額控除の扱いが変わるため |
| D-3 | 電帳法を意識したファイル名・検索要件（日付・金額・取引先）の付与 | 電子帳簿保存法の検索要件に対応 |
| D-4 | 「AIは案を出すだけ、確定は人間」の設計 | 内部統制上、AIに承認権限を持たせない |
| D-5 | 確信度が低い場合は明示的にフラグ | 誤仕訳の見逃し防止 |

## 4. 技術要件

### 4.1 技術スタック

| 層 | 採用技術 | 備考 |
|----|----------|------|
| フロントエンド | Next.js | Xserver VPS上でセルフホスト |
| APIサーバー | Python / FastAPI | PDF処理・AI呼び出し |
| PDF処理 | pdf2image, pdfplumber | 画像PDFは画像化してAIに渡す |
| AI | Gemini API | コスト抑制のため。既定モデル |
| DB | Supabase | 履歴・修正データの保存 |
| Webサーバー | Nginx（リバースプロキシ＋Basic認証） | |
| ホスティング | Xserver VPS（4GB / Ubuntu 24.04 LTS） | Vercelは使用しない |

### 4.2 LLM抽象化レイヤー（設計方針）

- AI呼び出しは `api/llm_client.py` に集約し、アプリ本体はモデル名を直接参照しない
- 既定はGemini。環境変数 `LLM_PROVIDER` でClaude等へ切替可能とする
- プロンプトはモデル非依存の形で管理
- 出力はJSONスキーマで検証し、モデル差替時の影響を最小化

### 4.3 非機能要件

| 項目 | 要件 |
|------|------|
| 公開範囲 | Basic認証（ID: eigyo）。パスワードは.env管理、リポジトリに含めない |
| レスポンス | 1件あたり読取〜仕訳案生成を30秒以内を目標 |
| コスト | 月あたりのAPI費用が無料枠内に収まることを目標 |
| セキュリティ | APIキーは.envで管理。.gitignoreで除外 |
| リソース | 既存24サービスと同居のため、メモリ使用量に留意 |

## 5. データ要件

### 5.1 勘定科目マスタ

- freee準拠
- 全科目は網羅せず、デモで頻出する経費系科目を中心に構築
- 拡張可能な形（JSON/DBテーブル）で保持

### 5.2 CSV出力

- freeeのCSVインポート形式に準拠
- **実装前に公式ヘルプで最新仕様（列名・順序・文字コード・日付形式）を確認すること**

### 5.3 サンプルデータ

- 架空企業のサンプル請求書を自作（テキストPDF／画像PDFの両方）
- 想定パターン: 通常課税仕入、軽減税率、非課税、インボイス番号なし、手書き風

## 6. 成果物

| ファイル | 内容 |
|----------|------|
| 要件定義.md | 本書 |
| README.md | サービス導入手順 |
| 振り返り.md | 開発で起きた課題と解決策 |

- GitHubリポジトリで管理
- プログラム変更はDiscordに記録
- 開発状況はNotionで整理

## 7. テスト要件

- 機能テスト（各機能の単体動作）
- **ブラウザからの実行テスト**（実際にアップロード〜CSV出力まで通しで確認）
- 仕訳精度の検証（サンプル10件程度で正答率を測定し記録）

## 8. スケジュール

| 週 | 内容 |
|----|------|
| 1週目 | 環境構築・サンプル請求書作成・PDF読取検証 |
| 2週目 | 仕訳推定プロンプト設計・精度検証 |
| 3週目 | 承認フローUI・CSV出力実装 |
| 4週目 | デモ公開・note/Zenn記事執筆・振り返り |

## 9. 未決事項

- [ ] サービス正式名称（開発完了前に決定）

## 10. 変更履歴

| 日付 | 内容 |
|------|------|
| 2026-08-09 | v1.0 初版作成 |
| 2026-08-09 | v1.1 実装着手。ポート割当(web:3100/api:8100)、メモリ上限、サンプル5件を追加 |
